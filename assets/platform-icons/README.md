# Platform Icons

Offline SVG brand icons used by Ting account cards and quick platform chips.

- Primary source: Simple Icons via jsDelivr (`simple-icons@16.16.0`, falling back to `@latest`)
- Downloader: `node scripts/download-platform-icons.cjs`
- Fallback icons are generated only when a brand icon is not available from the source package.
